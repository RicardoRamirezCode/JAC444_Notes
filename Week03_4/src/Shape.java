@FunctionalInterface
public interface Shape {
	// in an interface all methods are by default public and abstract
	double area();
	// public abstract double area(); // so a method with no implementation
}
